{
  "name": "carepulse-hospital-system",
  "version": "1.0.0",
  "description": "Intelligent Hospital Appointment and Queue Optimization System",
  "type": "module",
  "main": "backend/dist/server.js",
  "scripts": {
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "build:backend": "cd backend && npm install --include=dev && npm run build",
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && npm --prefix backend install --include=dev && npm --prefix backend run build",
    "start": "node backend/dist/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "keywords": ["hospital", "queue-management", "appointments", "react", "express", "socket-io"],
  "author": "CarePulse Health",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
